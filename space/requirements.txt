# Runtime deps for the Hugging Face Space (CPU). torch/torchvision are
# installed separately from the CPU wheel index in the Dockerfile.
# No chromadb — the Space uses the NumPy-backed in-memory store
# (src/multimodal_search/store/memory.py) loaded from parquet at startup.
sentence-transformers==5.6.0
open-clip-torch>=3.3
fastapi==0.138.1
uvicorn[standard]==0.49.0
pyarrow>=17
pillow>=11
numpy>=2
