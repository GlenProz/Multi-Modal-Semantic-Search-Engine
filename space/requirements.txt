# Runtime deps for the Hugging Face Space (CPU). torch/torchvision are
# installed separately from the CPU wheel index in the Dockerfile.
# Pinned to match the environment the parquet index was exported from.
chromadb==1.5.9
sentence-transformers==5.6.0
open-clip-torch>=3.3
fastapi==0.138.1
uvicorn[standard]==0.49.0
pyarrow>=17
pillow>=11
numpy>=2
