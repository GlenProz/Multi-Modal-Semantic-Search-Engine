# Base URL of the FastAPI backend (the Hugging Face Space).
# Server-only (no NEXT_PUBLIC_ prefix) — the browser never sees it; the
# Next.js API routes under app/api/* proxy to it.
# e.g. https://glenproz-nga-art-search.hf.space
ART_API_URL=
