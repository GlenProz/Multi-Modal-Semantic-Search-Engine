# Phase 1 — text-only MVP
pandas>=2.2
pyarrow>=15.0
requests>=2.32
tqdm>=4.66
python-dotenv>=1.0

sentence-transformers>=3.0
chromadb>=0.5

fastapi>=0.115
uvicorn[standard]>=0.30

# Phase 2 — multi-modal (CLIP)
# Install torch + torchvision from the CUDA wheel index FIRST:
#   pip install torch==2.11.0 torchvision --index-url https://download.pytorch.org/whl/cu128
# then `pip install -r requirements.txt` picks up the rest.
open-clip-torch>=3.3
pillow>=11
httpx>=0.28

# Phase 3
# streamlit>=1.36
# psycopg[binary]>=3.2
